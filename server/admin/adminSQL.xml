<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="adminSQL">
    <select id="selectProductList">
        SELECT ITEM_NO AS itemNo
              , ITEM_NM_1 AS itemNm1
              , ITEM_NM_2 AS itemNm2
              , PRICE AS price
              , ITEM_KCD AS itemKcd
              , ORIGIN_CD AS originCd
              , IMAGE_PATH AS imagePath
              , ITEM_DESC AS itemDesc
          FROM ITEMS
    </select>

    <select id="selectOneProduct">
        SELECT ITEM_NO AS itemNo
              , ITEM_NM_1 AS itemNm1
              , ITEM_NM_2 AS itemNm2
              , PRICE AS price
              , ITEM_KCD AS itemKcd
              , ORIGIN_CD AS originCd
              , IMAGE_PATH AS imagePath
              , ITEM_DESC AS itemDesc
          FROM ITEMS
         WHERE ITEM_NO = ${itemNo}
    </select>

    <insert id="registerNewProduct">
        INSERT INTO ITEMS
        (
          ITEM_NM_1
        , ITEM_NM_2
        , PRICE
        , ITEM_KCD
        , ORIGIN_CD
        , ITEM_DESC
        )
        VALUES
        (
          #{itemNm1}
        , #{itemNm2}
        , #{price}
        , #{itemKcd}
        , #{originCd}
        , #{itemDesc}
        )
    </insert>

    <update id="updateImagePath">
        UPDATE ITEMS
           SET IMAGE_PATH = #{imagePath}
         WHERE ITEM_NO = ${itemNo}
    </update>

    <update id="modifyProduct">
        UPDATE ITEMS
           SET ITEM_NM_1 = #{itemNm1}
              , ITEM_NM_2 = #{itemNm2}
              , PRICE = ${price}
              , ITEM_KCD = #{itemKcd}
              , ORIGIN_CD = #{originCd}
              , ITEM_DESC = #{itemDesc}
         WHERE ITEM_NO = ${itemNo}
    </update>

</mapper>
